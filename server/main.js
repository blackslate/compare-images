import { Meteor } from 'meteor/meteor';
import '../imports/api/collections/publisher' // for side-effects only
import '../imports/api/methods/broker' // for side-effects only

Meteor.startup(() => {

});
